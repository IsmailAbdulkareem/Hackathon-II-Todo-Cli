{{/*
This template documents the expected Secret structure but does NOT create it.
Secrets must be created manually before deploying this chart.

Create the secret with:
kubectl create secret generic todo-backend-secrets \
  --from-literal=DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require" \
  --from-literal=ANTHROPIC_API_KEY="sk-ant-..."

The deployment references this existing secret.
*/}}
